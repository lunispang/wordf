use crate::types::*;

pub struct Solver;

pub fn solve(puzzle: &Puzzle, solution: &Solution) -> Option<Solution> {
    //let last = 
    todo!()
}

impl Solver {
    fn solve(puzzle: &Puzzle) -> Solution {
        for (i, start_row) in puzzle.chars.iter().enumerate() {
            for (j, start_col) in start_row.iter().enumerate() {
                todo!()
            }
        }
        todo!()
    }
}
